#!/usr/bin/make -f

export PYBUILD_NAME=smfc

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Tests require IPMI hardware; skip during package build

override_dh_builddeb:
	dh_builddeb